<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FinanceTracker" parent="android:Theme.Material.Light.NoActionBar" />

    <!--
      The quick add card floats over whatever is behind it (usually the
      launcher), so the window itself draws nothing. The scrim and the card are
      Compose.
    -->
    <style name="Theme.FinanceTracker.QuickAdd" parent="Theme.FinanceTracker">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
